<%= form_for(@express_interst) do |f| %>
  <% if @express_interst.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@express_interst.errors.count, "error") %> prohibited this express_interst from being saved:</h2>

      <ul>
      <% @express_interst.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :postcode %><br />
    <%= f.text_field :postcode %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
